## PEC 4 Ej 1: respuestas

¿Qué realiza cada una de las siguientes opcinoes de Angular CLI?

* `ng new`:
* `ng generate`:
    * `component`:
    * `directive`:
    * `enum`:
    * `guard`:
    * `interface`:
    * `pipe`:
    * `service`:
* `ng serve`:
* `ng test`:
* `ng version`:

